package org.example.SOLID.ISP;

public class main {
    // basically segregation of the interface and not forcing client to implement methd which they never use so make it more modular so that anyone n future if require some part can use that directly else refactoring will be required
}
